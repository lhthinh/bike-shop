export enum ETypeService {
  BOOKING_AT_STORE = 0,
  BOOKING_AT_HOME = 1,
}

export enum EtypeServiceName {
  BOOKING_AT_STORE = 'Đặt lịch tại cửa hàng',
  BOOKING_AT_HOME = 'Tiếp nhận xe tận nơi',
}

export enum EUnit {
  DAY = 0,
  HOUR = 1,
  MINUTE = 2,
}

export enum EUnitName {
  DAY = "Ngày",
  HOUR = "Giờ",
  MINUTE = "Phút",
}
